{
  "name": "astro-cache-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "types": "wrangler types --env-interface CloudflareBindings --experimental-include-runtime ./worker-runtime.d.ts",
    "preview": "wrangler dev",
    "deploy": "wrangler deploy --minify"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "wrangler": "^3.109.1"
  }
}
